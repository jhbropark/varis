# varis.kr 구조 분석 (2026-07-31 기준)

실제 배포본을 읽어 정리한 현행 구조. `varis/index.html` 원페이지의 근거 자료.

## 1. 도메인 구성

허브 1개 + 랜딩 서브도메인 4개. 전부 Vercel 정적 호스팅, 동일 브랜드 시스템
(다크 `#15171c` · 액센트 앰버 `#e9a23c` · Pretendard + 모노 · `◤◢` 로고마크).

| 도메인 | 역할 | 티어 |
|---|---|---|
| `varis.kr` | 허브 — 진단 + 과정 카탈로그 + 작품 | — |
| `vod.varis.kr` | AI 미디어아트, 첫 한 편 (4주 VOD) | 입문 / 저가 |
| `ai.varis.kr` | VARIS AI — AI 영상 도구 30개 | 입문~중가 |
| `pro.varis.kr` | 이머시브 미디어아트 & 테크 전문가 과정 (6개월) | 중가 / 코호트 |
| `houdini.varis.kr` | 제작 멘토링 (코칭 / Elite / Premium 1:1) | 중가~고가 |

공통 요소: Formspree `mvznljpl` 단일 엔드포인트, `varis@varis.kr`,
서울 상암 DDMC, `utm_*` + referrer 를 sessionStorage 에 저장하는 first-touch 추적.

## 2. 페이지별 섹션 구조

### varis.kr (허브)
헤더 → 히어로(`어디서 시작하든`) → **30초 진단**(`#vs-finder`, Q3개 가중치 스코어링)
→ 전체 과정 카탈로그 4장(`#vs-catalog`) → WORKS · SIA.HAUS 5작품 → 상암 DDMC 밴드 → 푸터(뉴스레터)

### pro.varis.kr
히어로 캐러셀 3슬라이드 → 상담/첨삭/취업연계 3열 → CURRICULUM 4STEP →
SOFTWARE 4종 → 프로그램 밴드(4 STEPS / 6 MONTHS / 20 SEATS) → 쇼릴 → WORKS →
비주얼 컬렉션 → 모집 안내 7항목 → FAQ 4 → 지원 폼

### vod.varis.kr
히어로(얼리버드 ₩39,000) → 페인/게인 대조 → 결과물 → 4주 커리큘럼(주차별 3레슨 + 산출물)
→ 대상 4 → 가격(정가 ₩69,000 → ₩39,000, 43% OFF, 무통장 + 포트원 카드결제) →
밸류래더 업셀 → FAQ 5 → 신청 폼

### houdini.varis.kr
히어로 → 문제 제기 → 과정 소개(Blender→Houdini→AE 파이프라인) → 목표 7 → 상암 →
**과정 3옵션 상세** → 비교표 → 국비 대비 우위 → CTA → 신뢰 → 희소성 → 중고신입 포지셔닝 →
6개월 커리큘럼 → 결과물 5 → 추천 대상 9 → 주의 문구 → FAQ 6 → 상담 폼

### ai.varis.kr
히어로(BRIEF→SELECT→OUTPUT 파이프) → 스탯 → WHY 5단계 → 학습경로 5(하위 페이지 `/paths/*`)
→ 30개 도구 4카테고리(하위 페이지 `/tools/*`) → 패스트스타트 → 4주 랩 → 살아있는 강의 →
실무 문서 8종 → 가격 4티어 → FAQ 4 → 신청 폼

## 3. 가격 체계 (사이트 표기 그대로)

| 과정 | 가격 |
|---|---|
| VOD · 첫 한 편 | 얼리버드 ₩39,000 (정가 ₩69,000) · 평생소장 |
| VARIS AI · FREE | 0원 (20분 미니 프로젝트) |
| VARIS AI · CORE | 149,000원~ |
| VARIS AI · PROJECT | 390,000원~ (4주 코호트) |
| VARIS AI · TEAM/B2B | 별도 견적 |
| 온라인 코칭 | 1회 15만 / 4회 50만 / 월간 60만 |
| Elite Small Group | 월 35만 × 6 (얼리버드 198만 · 정가 240만) |
| Premium 1:1 | 월 65만 × 6 (얼리버드 390만 · 정가 480만) |
| 전문가 과정(pro) | **가격 미표기** — 상시모집, 1:1 면접 |

## 4. 파악된 불일치 (원페이지에서 정리한 것)

1. **진단기가 VOD를 추천하지 않음.** 허브 `#vs-finder` 의 `COURSES` 는
   `ai / coach / pro / elite / prem` 5개뿐이라, 최저가 입문 상품인 `vod.varis.kr` 가
   어떤 응답 조합으로도 결과에 뜨지 않는다. 정작 바로 아래 카탈로그에는 첫 카드로 노출된다.
2. **ai.varis.kr 설명 불일치.** 진단기는 `AI 도구 30개 8주`로 안내하지만
   실제 사이트는 4주 코호트 + CORE 149,000원~ 이다.
3. **전문가 과정 CTA 가 허브로 되돌아감.** 진단 결과의 `pro` 링크가
   `https://varis.kr/#apply` 인데 허브에는 `#apply` 앵커가 없다 (신청 폼은 pro 에만 있음).
4. **가격 축이 끊김.** pro 만 금액 비표기라 사다리를 금액순으로 비교할 수 없다.

원페이지에서는 진단기에 VOD 를 추가하고, `ai` 설명을 실제 값으로 맞추고,
모든 CTA 를 같은 페이지 내 앵커(`#apply`)로 통일했다.
